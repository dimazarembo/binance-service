server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: binance-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true